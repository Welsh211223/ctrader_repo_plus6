[pytest]
addopts = -q --cov=ctrader --cov-report=term-missing:skip-covered
testpaths = tests
norecursedirs =
    .venv
    build
    dist
    node_modules
    .*
filterwarnings =
    ignore:lib2to3 package is deprecated
