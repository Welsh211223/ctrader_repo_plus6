# Contributing

- Use feature branches and open PRs to `main`.
- Ensure pre-commit hooks pass locally: `pre-commit run --all-files`
- No secrets in code or tests. Use environment variables or GitHub Environments.
