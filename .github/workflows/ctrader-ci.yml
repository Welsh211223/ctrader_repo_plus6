name: ctrader-ci

env:
  PYTHONPATH: src

on:
  pull_request:
  push:

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - name: Install package (editable)
        run: python -m pip install -e .
      - uses: actions/checkout@v4
      - name: ctrader-ci smoke
        run: echo "ctrader-ci ok"




